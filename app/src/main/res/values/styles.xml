<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <ITransaction name="colorPrimary">@color/colorPrimary</ITransaction>
        <ITransaction name="colorPrimaryDark">@color/colorPrimaryDark</ITransaction>
        <ITransaction name="colorAccent">@color/colorAccent</ITransaction>
    </style>

</resources>
