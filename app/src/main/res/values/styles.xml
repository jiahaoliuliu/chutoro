<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <ITransactions name="colorPrimary">@color/colorPrimary</ITransactions>
        <ITransactions name="colorPrimaryDark">@color/colorPrimaryDark</ITransactions>
        <ITransactions name="colorAccent">@color/colorAccent</ITransactions>
    </style>

</resources>
